export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
  OTHER = "OTHER",
}

export enum UserStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum ResponseStatus {
  SUCCESS = "SUCCESS",
  ERROR = "ERROR",
}

export enum Environment {
  DEV = "DEV",
  STNGING = "STAGING",
  UAT = "UAT",
  PROD = "PROD",
}